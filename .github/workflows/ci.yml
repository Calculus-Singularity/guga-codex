name: CI

on:
  pull_request: {}
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  fmt:
    name: Format
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: codex/codex-rs
    steps:
      - uses: actions/checkout@v4

      - name: Checkout submodules
        shell: bash
        working-directory: .
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SUBMODULE_TOKEN: ${{ secrets.SUBMODULE_TOKEN }}
        run: |
          token="${SUBMODULE_TOKEN:-$GITHUB_TOKEN}"
          git -c url."https://${token}@github.com/".insteadOf=git@github.com: submodule update --init --recursive

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: cargo fmt
        run: cargo fmt -- --check

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: codex/codex-rs
    steps:
      - uses: actions/checkout@v4

      - name: Checkout submodules
        shell: bash
        working-directory: .
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SUBMODULE_TOKEN: ${{ secrets.SUBMODULE_TOKEN }}
        run: |
          token="${SUBMODULE_TOKEN:-$GITHUB_TOKEN}"
          git -c url."https://${token}@github.com/".insteadOf=git@github.com: submodule update --init --recursive

      - name: Install Linux build dependencies
        working-directory: .
        run: sudo apt-get update && sudo apt-get install -y libcap-dev

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache cargo registry and build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: codex/codex-rs -> target
          shared-key: ci-clippy

      - name: cargo clippy
        run: cargo clippy -p codex-core -p codex-tui -p codex-cli --all-targets

  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: codex/codex-rs
    steps:
      - uses: actions/checkout@v4

      - name: Checkout submodules
        shell: bash
        working-directory: .
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SUBMODULE_TOKEN: ${{ secrets.SUBMODULE_TOKEN }}
        run: |
          token="${SUBMODULE_TOKEN:-$GITHUB_TOKEN}"
          git -c url."https://${token}@github.com/".insteadOf=git@github.com: submodule update --init --recursive

      - name: Install Linux build dependencies
        working-directory: .
        run: sudo apt-get update && sudo apt-get install -y libcap-dev

      - uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry and build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: codex/codex-rs -> target
          shared-key: ci-test

      - name: cargo test
        run: cargo test -p codex-core -p codex-tui -p codex-cli --lib
        env:
          RUST_BACKTRACE: 1
